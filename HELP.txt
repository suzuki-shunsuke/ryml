  Usage: ryml [options] [<method> <url>]

  Args:

    method      http method
    url         url

  Options:

    -h, --help         output usage information
    -v, --version      output the version number
    -q, --quiet        don't output the command
